server:
  port: 8888

security:
  basic:
    enabled: false

spring: 
  application: 
    name: config-service
  cloud:
    config: 
      label: dev
      server: 
        git: 
          uri: //${user.dir}/../hydra-configurationBAM